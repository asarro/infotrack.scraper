services:
  ui:
    build:
      context: ./infotrack-ui
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    networks:
      - infotrack-network

networks:
  infotrack-network:
    name: infotrack-network
